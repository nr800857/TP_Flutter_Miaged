class User {
  String mail;
  String password;
  String birthday;
  String address;
  String postcode;
  String city;

  User(this.mail, this.password, this.birthday, this.address, this.postcode, this.city);
}